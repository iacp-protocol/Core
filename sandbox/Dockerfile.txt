# C:\Users\bronn\Core\sandbox\Dockerfile
FROM python:3.12-slim

WORKDIR /app

# dependances
COPY ./requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# code (src facultatif pour plus tard)
COPY ./src ./src
COPY ./sandbox ./sandbox

ENV PYTHONPATH=/app

# CMD par défaut (sera remplacée par docker-compose.yml)
CMD ["python","-m","uvicorn","sandbox.agent_app:app","--host","0.0.0.0","--port","8000"]

